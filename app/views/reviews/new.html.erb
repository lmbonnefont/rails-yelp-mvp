<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.label :rating %>
  <%= f.select :rating, [0, 1, 2, 3, 4, 5], label: "rating" %>
  <div class="bouton">
    <%= f.button :submit, "save", class:"btn btn-primary" %>
  </div>
<% end %>
